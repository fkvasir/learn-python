class student: # overview of the *student*

  def __init__(self, name, major, GPA, is_on_suspension): # initialyzing function # classifications
    """
    ... self.name -> the name of the student is stored in the name that we passed in
    so on and so forth
    """
    self.name = name
    self.major = major
    self.GPA = GPA
    self.is_on_suspension = is_on_suspension
    