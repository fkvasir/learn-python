name = ["L","y","n","n","i","e"]
w = ord(name[0])
x = ord(name[1])
y = ord(name[2])
z = ord(name[3])

def compute():
  if x == x:
    k = (x*x*x)/2
    l = 5*float(k)
    m = x*x
    n = 3*float(m)
    h = float(l)+float(n)+11
    return h
