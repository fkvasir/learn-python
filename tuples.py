# a TUPLE is a type of data structure which basically means a container where we can store different values
coordinates = (4, 5) 
"""
a TUPLE is immutable
The tuple cant be changed or modified
"""
print(coordinates[0]) 
print(coordinates[1])
# tuple-is-different-from-list